******** OpenSerialDataExplorer README ***********  18 Apr 2009 WB

Der OpenSerialDataExplorer (OSDE) wird ohne jegliche Gew�hrleistung, daf�r aber kostenfrei und ohne Spende zur Verf�gung gestellt. 
Durch die Implementierung in Java auf der Basis von der freien Bibliothek RXTXcomm und dem freien Eclipse SWT ist die Ausf�hrung
des Programms auf Windows, Linux und anderen Plattformen m�glich. Voraussetzung ist eine Java Version 1.6 oder h�her. 
Getestet und gebaut wird zu Zeit nur die Version f�r Windows und Linux in 32 und 64 Bit. Die Version f�r Mac OS sollte prinzipiell
laufen kann aber mangels Mac OS nicht getestet werden.
Der OpenSerialDataExplorer nimmt Daten von seriell angeschlossenen Ger�ten entgegen, zeigt diese f�r weiteres Auswerten an. 
Zur Zeit sind folgende Ger�te eingebunden: 
											 - AkkuMasterC4 (Htronic)
											 - Picolario (Renschler)
											 - UniLog (SM-Modellbau)
											 - LiPoWatch (SM-Modellbau)
											 - eStation BC6, BC610, BC8, 902 (Bantam)
											 - Pichler P6 , P60 (Pichler)
											 - VC820, VC840 (Conrad/VoltCraft)
											 - Simulator
											 
Weitere Ger�te sind in Arbeit. Hilfe bei der Einbindung von zus�tzlichen Ger�ten ist herzlich willkommen.

Eine Installation ist nicht n�tig, das im Archiv enthaltene Verzeichnis "OpenSerialDataExplorer" mit den darunter liegenden Dateien
einfach an einen Ort entpacken, wo Ausf�hrungsrechte vorliegen. 
F�r Windowsbenutzer w�re das z.B. %ProgramFiles% f�r Linuxbenutzer "/opt" oder "/usr".

F�r den ersten Start des Programms �ber ein Kommando-Fenster(Windows) Shell(Linux) in das OpenSerialDataExplorer Verzeichnis 
wechseln und unter Windows, OpenSerialDataExplorer.exe oder Unix, OpenSerialDataExplorer, ausf�hren. 

Unten dem OpenSerialDataExplorer Programmverzeichnis liegt folgende Verzeichnisstruktur:
OpenSerialDataExplorer\OpenSerialDataExplorer.exe (Windows)
OpenSerialDataExplorer\OpenSerialDataExplorer.ini (Windows)
OpenSerialDataExplorer\OpenSerialDataExplorer (Linux)
OpenSerialDataExplorer\OpenSerialDataExplorerMac32 (Mac OS)
OpenSerialDataExplorer\OpenSerialDataExplorer.xpm (Linux, Mac OS)
OpenSerialDataExplorer\OpenSerialDataExplorer.desktop (Linux)
OpenSerialDataExplorer\OpenSerialDataExplorer.jar
OpenSerialDataExplorer\Devices\*.jar

Erweiterung der Java Laufzeitumgebung f�r RXTXcomm und SWT:
OpenSerialDataExplorer\java\ext\*.jar
OpenSerialDataExplorer\java\ext\*.LICENSE

Hinweis f�r Linux-Benutzer: RxTxcomm ist verwendet den uucp locking Mechanismus, hierzu muss der Benutzer der Gruppe "uucp" angeh�ren. In Abh�ngigkeit
der verwendeten Distribution kann evtl. kein serieller Port ge�ffnet werden.

Beim ersten Start des Programms werden dann noch Anwendungkonfigurationsverzeichnisse unter:
%APPDATA%\<Benutzer>\OpenSerialDataExplorer (Windows)
/home/<Benutzer>/.OpenSerialDataExplorer (Linux)
/user/<Benutzer>/.OpenSerialDataExplorer (Mac OS)
angelegt. Hierunter werden auch Anwendungkonfiguration und Tracelogdaten abgelegt.

F�r das Verzeichnis der Nutzerdaten (Logdateien) ist der Anwender selbst verantwortlich, es wird nach dem ersten Start des Programms
in den Anwendungkonfigurationsdatei (OpenSerialDataExplorer.properties) hinterlegt.

Hinweis: Erscheint nach der Ausf�hrung von OpenSerialDataExplorer.exe/OpenSerialDataExplorer folgende oder �hnliche Fehlermeldung 
"The java class could not be loaded. java.lang.UnsupportedClassVersionError: (osde/OSDE) bad major Version at offset=6"
ist die installierte Java Version (java -version) zu alt und muss gegen eine aktuelle Version > 1.6 ausgetauscht werden.
F�r Windowsanwender: Je nach verwendeter Windows Version und Patchlevel kann es n�tig sein das System mit dem 
"Microsoft Visual C++ 2008 Redistributable Package" von www.microsoft.com/downloads nachzur�sten (Desktopicon, Objektlink).

Um Fehler oder Anregungen zu melden, bitte eine e-Mail an die Adresse auf der Downloadseite.
